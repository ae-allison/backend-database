{
  "name": "fades-of-gray",
  "version": "1.0.0",
  "description": "Fades of Gray appointment system",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && export NODE_ENV=production && node dist/server.js",
    "dev": "npm run build && NODE_ENV=develop node dist/server.js",
    "set": "NODE_ENV=development node script/startFadesOfGray.js",
    "test": "tsc && export NODE_ENV=develop && mocha --recursive dist/tests --exit"
  },
  "repository": {
    "type": "git"
  },
  "author": "Anson Ervin",
  "license": "MIT",
  "bugs": {},
  "dependencies": {
    "firebase-admin": "^8.9.2",
    "uuid": "^3.3.2"
  },
  "devDependencies": {
    "@types/express": "^4.16.1",
    "@types/mocha": "^5.2.6",
    "@types/node": "^11.11.6",
    "mocha": "^6.0.2",
    "request": "^2.88.0",
    "sinon": "^7.3.2",
    "supertest": "^4.0.2",
    "typescript": "^3.3.4000"
  }
}
